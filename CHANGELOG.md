# Changelog

本项目的版本发布记录。日期以 APK 打包日为准。
版本号采用三段式（MAJOR.MINOR.PATCH），versionCode 顺次 +1，不跳号。

## [1.3.0] - 2026-09-17

### 新增

- **中英双语（i18n）**：界面与语音播报支持中文/English——默认跟随系统语言，⋮ 菜单「语言」可手动切换并记忆；`L10n.kt`（Strings 中/英双套 + 阶段/方向名查表本地化）+ `L10nStore.kt`（auto/zh/en 持久化）；UI/弹窗/引擎播报/锻炼要点/通知/TTS（zh-CN / en-US）全链路覆盖。
- **打卡按模式统计 v2**：`CheckInLog` 每天携带模式位掩码（`v2:base36` 序列化，v1 旧数据兼容解析）；日历新增分模式计数行（本月 + 累计）与打卡格模式小圆点（青=舒缓、橙=等长抗阻，双 done 双点）。
- **英文版 `README_EN.md`**（与中文版互链）；界面截图更新为中/英两套。

### 变更

- **舒缓锻炼改为右手开始**（原左手先），三处 JSON 配置同步。
- **首页布局**：模式卡 + 打卡日历行改为在标题与页脚间垂直居中（上下等重 weight），两侧留白 24→16dp——修复内容堆积在屏幕上半部、下方 43% 空白的问题（uiautomator 实测卡片块 29%–52% → 45%–67%）。
- 通知文案与渠道名随应用语言；`Model.tips` 并入 `L10n`；单元测试 48 → 63 例（新增 `L10nTest`）；版本 1.2(3) → 1.3.0(4)。

## [1.2] - 2026-09-12

### 修复

- **每日提醒"不如期"触发**：闹钟机制由 `setInexactRepeating`（系统文档允许首次触发延迟近一个完整周期，即最多一天）重构为 `setAlarmClock` 精确闹钟链——准点触发、可穿 Doze；接收器触发后自动续期次日，重启由 BootReceiver 重排，App 启动时兼容性重排旧闹钟。实测模拟器 `02:21:00.000` 精确触发出通知并自动续期次日。
- **首页滑动卡顿**：入场瀑布动画（逐块 slide 触发布局重测，实测 65% 卡顿帧）改为整体纯淡入；GPU 渲染下持续动画 59fps / 0.9% 卡顿。

### 变更

- 首页重排：紧凑头部（图标 chip + 标题同行）、双锻炼卡并排、打卡日历降为紧凑次级行并显示实时连续天数。
- 版本号 1.1(2) → 1.2(3)，新增 `USE_EXACT_ALARM` 权限。

## [1.1] - 2026-09-11

### 新增

- **打卡日历**：完成锻炼自动打卡（按天去重）、月历视图（今日描边高亮）、连续/最长 streak（当天未练不断签）、里程碑徽章（7🥉/30🥈/100🥇/365👑）。
- **打卡持久化**：`CheckInStore`（SharedPreferences）修复划掉 App/冷启动丢历史问题；`v1:base36` 紧凑序列化。
- **每日提醒**（初版）：时间选择 + 通知 + 已打卡跳过 + 开机重排（1.2 中重写了触发机制）。
- **⋮ 溢出菜单**：收纳打卡日历/锻炼要点/计时设置/关于（含版本号），自绘卡片 + 弹出动效 + 背景压暗。
- **计时设置**：运行时步进器编辑准备/发力/放松/组数，`TimingConfig.toJson` 序列化 + `TimingStore` 持久化，热生效。
- **屏幕常亮**：锻炼运行期间 `FLAG_KEEP_SCREEN_ON`。
- **全局动效**：各页错峰淡入、阶段文字过渡、进度条展开、翻月交叉淡入。

### 修复

- 舒缓模式进度条 16 格修正为 8（`groups` 语义统一为"每方向组数"，引擎完成条件 `dirs × groups`，Web 版同步修复）。
- 跨午夜后完成锻炼漏记打卡（守卫由布尔改为按日期）。

## [1.0] - 2026-09-11

首个可用版本。

- 双锻炼模式（舒缓 8s×8 轮 / 等长抗阻 3 阶段 15 组）、TTS 语音 + 音效引导、锚点计时零漂移、暂停/重置、锻炼要点弹窗。
- 纯 Kotlin `WorkoutEngine` 状态机 + 手写 JSON 时长配置解析器，零第三方依赖。
